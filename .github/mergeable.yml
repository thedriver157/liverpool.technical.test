version: "1.1"
rules:
  - name: "Validar nombre de la Pull Request"
    pull_request:
      types: [opened, edited]
    title:
      # Expresión regular: debe empezar con un prefijo convencional
      must_match: "^(FEAT|FIX|DOCS|REFACTOR):"